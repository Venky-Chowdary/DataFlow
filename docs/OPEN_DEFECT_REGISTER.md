# Open-defect register — consolidated across the six-session wave

Every item the wave left open, in one list, taken from the sessions' own
evidence documents after all track branches merged into `feature/Venkat-Analysis`:
`ALL_SESSIONS_HANDOVER.md` §4–6, `SCALE_MATRIX_SQL.md` (A), `SCALE_MATRIX_FILES.md`
(B), `SCALE_MATRIX_NOSQL.md` (C), `SCALE_MATRIX_MODES_SCHEDULES.md` (D),
`CONNECTOR_READINESS_MATRIX.md` (E).

Three columns are kept apart on purpose, because collapsing them is how a
product gets called ready when it is not:

* **defect** — the product is wrong, and the owner module is named.
* **not measured** — the product may be right; nobody proved it.
* **environment** — cannot be proven on this box (no credentials, no service).

A defect is closed here only when a *real* transfer on a live engine proves it,
with the destination read back on a connection the transfer engine never
touched. A passing unit test is not a closure.

---

## 1. Defects — ordered by blast radius

| # | Defect | Owner module | From | Evidence of the failure |
|---|--------|--------------|------|--------------------------|
| ~~D1~~ | **Closed.** A schemaless destination's shape was inferred from a bounded value sample and then compared as if the destination had *declared* it, so run 2 of a route refused what run 1 correctly wrote. Authority now leaves the probe: **sampled** (object store / SFTP / Redis / document stores) is a profile that *widens* to the source declaration before fidelity and write; **declared** (SQL DDL, Elasticsearch mapping, operator override) stays enforced; a failed probe (MinIO `NoSuchKey`) stays unread (`dest_type_unread`), never invented-compatible. Suppressing the Map verdict alone would have failed open — writers still enforce probed widths, so the write path widens the same profile. `is_lossy_coercion`, confidence floors, and the Mongo/Dynamo `dest_decimal_single_capacity_digits` exemption are unchanged. D16 is not regressed: Elasticsearch `native_types` stay declared. | new `services/dest_schema_authority.py` + `src/transfer/engine.py` + `src/transfer/endpoint_intelligence.py` + `connectors/object_store_common.py` + `services/mapping_pipeline.py` + `services/decision_kernel/invent.py` | E (Family A), C (D5) | 23 cases in `tests/test_dest_schema_authority_d1.py` (sampled vs declared, repeated-run, narrow first sample, probe failure, D16 identity 0.99, document-store exemption, JSON GET dest sample) + live `tests/test_d1_postgres_minio_repeated_run_live.py` on local Postgres+MinIO **2026-09-01**: run 1 transferred 2 rows, independent boto3 GET of `exports/d1_src_*.json` re-read amounts `12.50`/`100.00`; the product's own dest probe still *measured* `{"amount": "DECIMAL(4,1)", …}` (a width nothing declared) but stamped `schema_authority.amount=sampled`; Map graded `amount` `preserve` @ **0.99** origin `sampled_profile` (widened to `DECIMAL(38,2)`); run 2 transferred 2 rows, 0 rejected, independent reread matched. Redis/ES live cells were not run on this box (ports 6379/9200 down). Real S3/GCS stay §3 (no credentials). |
| ~~D2~~ | **Closed.** A SQL Server create-new stamped code-page `VARCHAR` for a source column that provably holds Unicode, and then honestly refused `U+8A9E` at the write. The carrier is now decided from the source column's *own measured* declaration, not the engine name alone: a proven Unicode-capable column lands `NVARCHAR(n)` / `NCHAR(n)` / `NVARCHAR(MAX)`, a genuinely code-page-bound one (`CHARACTER SET latin1`) stays `VARCHAR`, a SQL Server `_UTF8` collation stays a Unicode-capable `VARCHAR`, and an unmeasured declaration stays conservative. Three carriers of that evidence were being destroyed on the way: `ddl_carrier_type` dropped the trailing `CHARACTER SET`/`COLLATE` clause, `normalize_logical_type` missed every qualified MySQL LOB, and `is_fixed_width_char_carrier` read the word `CHARACTER` in `CHARACTER SET` as the ANSI `CHARACTER` carrier — so a Unicode column was reported as a blank-padded `VARCHAR` and the required promotion was graded `precision_collapse`, failing G4. SQL Server collation validation also now requires the sensitivity suffix, so MySQL spellings (`latin1_swedish_ci`) are refused instead of reaching the CREATE. | `services/type_system.py` + `services/decision_kernel/type_invent.py` + `services/dest_dialect_facts.py` | A | live `mysql → sqlserver` in `tests/test_sqlserver_unicode_carrier_live.py`: a mixed-charset source (`utf8mb4` `VARCHAR`/`LONGTEXT` + `latin1` `VARCHAR`) transfers, `sys.columns`/`sys.types` on an independent connection show `nvarchar`/`nvarchar(max)`/`varchar`, `語` and `😀` re-read byte-exact, the source re-reads unchanged, and a direct probe proves the old carrier was lossy (`VARCHAR` reads `語` as `?`, `NVARCHAR` exact); plus 40 focused cases in `tests/test_sqlserver_unicode_carrier_invent.py` |
| ~~D3~~ | **Closed.** `* → sqlite` graded `UUID → TEXT` / `UUID → VARCHAR` / `CHAR(36) → TEXT` as `precision_collapse` and demanded a signed Migration Risk Contract — a refusal that described a carrier SQLite does not have. SQLite gives `CHAR(36)`, `VARCHAR(36)` and `TEXT` one TEXT affinity: the declared length is parsed and discarded, nothing is blank-padded, and no carrier the dialect can spell enforces a UUID domain, so `VARCHAR(36)` there enforces nothing `TEXT` does not and the value is carried byte-exact. The fidelity gate now asks the *destination* whether a narrower text carrier exists (`dest_string_length_is_unenforced` / `uuid_carrier_is_dialect_equivalent`), and the same rule answers Map, the coercion preflight, Execute and the create-new risk stamp. What the operator loses is still stated, not hidden: create-new stamps a warn-level `uuid_carrier_equivalent` (values round-trip exactly, the UUID domain is not enforced) and `fixed_width_not_enforced` for a fixed-width source, both informational so auto-map Execute is not failed as `g4_mapping_confidence`. Every other dialect keeps its previous grading — `UUID → TEXT` still blocks on PostgreSQL / MySQL / Snowflake / BigQuery / SQL Server, and `UUID → INTEGER` still blocks on SQLite. | `services/type_system.py` + `services/dest_dialect_facts.py` + `services/type_coercion_validator.py` + `services/create_new_risk_stamp.py` | A | 39 cases in `tests/test_sqlite_text_carrier_equivalence_d3.py`, incl. a live `postgresql → sqlite` transfer of `uid UUID` + `code CHAR(36)` into a **created** SQLite file: `PRAGMA table_info` on an independent connection shows `uid TEXT`, `typeof(uid) = 'text'` with both UUIDs byte-exact, the source re-reads unchanged, and the preflight raises no issue and no contract demand (the same mapping on Snowflake still blocks). The 18 previously failing `* → sqlite` cells are the same route |
| ~~D4~~ | **Closed.** Invented MySQL DDL emitted a `CHARACTER SET … COLLATE` clause the server rejects: on a `JSON`/`LONGBLOB` target that is `1253` (collation not valid for CHARACTER SET 'binary'), on a numeric or temporal target `1064`, and on a column whose type already fixed its charset it is a second clause plus a cross-charset collation. Character clauses are now planned only for character carriers, only from the charset the column actually stores, and the equality question on a non-character target is answered `unsupported` naming that target instead of being dropped silently. | `services/collation_carry.py` + `services/decision_kernel/type_invent.py` | A | `tests/test_mysql_charset_collation_ddl.py` (15 cases) + live `tests/test_mysql_charset_collation_live.py`: representative `sqlserver → mysql` and `oracle → mysql` catalogs render a CREATE the local server accepts, with `information_schema` showing a charset on the character columns and none on the numeric/temporal/binary ones |
| ~~D5~~ | **Closed.** Two halves. (a) A cross-engine mapping that cannot preserve the source's equality polarity is now refused in words (`CI_AS` is accent-sensitive, `unicode_ci` is not) rather than replaced by the nearest-named collation, and same-engine invention carries the source collation on the type, before `NOT NULL`. (b) MySQL `NVARCHAR`/`NCHAR` are aliases for `CHARACTER SET utf8mb3` (BMP-only), not a national wire: reading the name as national promised UTF-16 capacity, so a SQL Server `NVARCHAR` source graded `carried` and its first astral scalar died at the write with `1366`. Capacity is measured as utf8mb3, and a create-new target fed by a genuinely wider national source states `CHARACTER SET utf8mb4`; a MySQL-family source keeps its own spelling, since it has no wider repertoire to carry. | `services/collation_carry.py` + `services/encoding_capacity.py` + `services/type_system.py` | A | live MySQL: astral scalar inserted through the national route re-reads byte-exact on an independent connection with the source's `_bin` equality intact, and the MySQL-source alias is measured refusing the same scalar with `1366` |
| ~~D6~~ | **Closed.** PostgreSQL destination key census failed with `operator does not exist: text = integer` — the census bound source-spelled key literals against a typed destination column. The census now reflects the destination's declared key types (DB-API catalog and SQLAlchemy paths) and compares in the *destination's* domain, keeping the comparison index-usable; a key the column cannot hold (`'abc'`, `22.4` into `integer`) is a proven miss, not an error. | `services/dest_key_typing.py` (new canonical owner) + `services/dest_precount.py` | C | live repro on the compose Postgres/MySQL, then 28 regression cases incl. 4 live ones in `tests/test_dest_key_census_typing.py` |
| ~~D7~~ | **Closed.** Elasticsearch wrote `DECIMAL` as a precision-preserving string with no explicit mapping, so dynamic mapping made the field `text` (not aggregatable, not rangeable) and the next run read that `text` back as this route's own destination carrier and refused its own output. A created index now declares every mapped field from the **source** carrier (Map can only stamp an ES field type, which cannot express `DECIMAL(12,4)`): bounded decimals become fixed-point `scaled_float`, wider-than-exact ones stay exact `keyword`, and `meta.df_carrier` records the carrier so a reread recovers it. An existing index whose new fields cannot be declared now fails the write instead of falling through to dynamic mapping. Two further defects found while proving it: the live client answers `get_mapping` with `ObjectApiResponse` (not a `dict`), so **every** already-declared field read as undeclared — existing-index carriers were never read from a live server; and an ES `date` field holds an *instant*, so it no longer binds as SQL `DATE` (time of day truncated) and an offset binds as an instant instead of being dropped into a wall clock hours away. | `connectors/elasticsearch_mapping.py` (new) + `connectors/elasticsearch_writer.py` + `services/schema_introspect.py` + `services/type_system.py` | E | 23 cases in `tests/test_elasticsearch_decimal_mapping.py`, incl. a live `postgresql → elasticsearch` transfer proving `scaled_float(10000)` / exact `keyword` / `date`, dest `COUNT(*) = 3`, exact `_source` values, a range query over the decimal, a second run that does not reinterpret the fields, and an independent reread through the product's own introspection |
| ~~D8~~ | **Closed.** The bare token `long` was read as Oracle's deprecated text LOB, so an ordinary INT64 copy (`mongodb`/`spark`/`iceberg`/`elasticsearch` `long → BIGINT`) was flagged as an invented numeric domain and stopped for an approval it never needed. The meaning now belongs to the *source engine*: Oracle's `LONG` stays a text LOB (and lands the destination's own text carrier, not `BIGINT`), every other engine's `long` is INT64 and converts as equivalent, and an **unknown** source gets neither guess — it keeps the conservative refusal and the historical carrier. | `services/type_system.py` (`source_long_is_int64` / `source_long_is_text_lob`) + `services/decision_kernel/type_invent.py` | E | 43 cases in `tests/test_long_token_source_identity.py` over conversion class *and* invented DDL; the 14 pre-existing failures in §1 D17 reproduce identically on the base commit, so nothing regressed |
| ~~D9~~ | **Closed.** The hubspot / salesforce / airtable / kafka / shared-SaaS verifiers read `limit or 500` rows, so a whole-population reconcile hashed a 500-row prefix against the source's whole-table digest and reported `500` as a destination population nobody counted — a mismatch for every correct load past 500 rows, and a pass-shaped count for a partial one. `limit = 0` now means the whole population (read to a 100,000-row ceiling, requested as ceiling + 1 so a larger population is *detectable*), an explicit caller limit is still honoured exactly, and a read-back that could not see the whole population returns **unproven** rather than offering a prefix digest as the destination checksum. Proving it exposed a second, worse defect in the Salesforce reader: it clamped the SOQL `LIMIT` to the 2,000-row **OFFSET** cap, which is not a limit on how many rows a query may return — every object past 2,000 records was silently truncated to its first 2,000 on any read, transfer included, while looking successful. `LIMIT` is now emitted only below the SOQL row ceiling and `queryMore` walks the rest. | `services/reconciliation.py` (`_saas_readback_limit` / `_saas_readback_truncated`) + `connectors/salesforce.py` | E | 5 rule cases in `tests/test_saas_reconcile_population_readback.py`; live over real HTTP against the Salesforce double in `tests/test_saas_reconcile_live_readback.py` — a 2,300-record object counted as 2,300 and digested whole across a `queryMore` boundary (the double now pages in 2,000-record batches as the org does), with the old 500-row prefix digest shown to be a *different* value. Hosted HubSpot / Airtable / Kafka populations remain unproven against real vendor endpoints (no credentials here); the rule is covered by stubbed read-backs |
| ~~D10~~ | **Closed by re-measurement.** The `postgresql → excel` cell now passes on the compose Postgres at 100,000 rows: independent read-back of the landed workbook counts 100,000 rows and its checksum is byte-identical to the seeded population's own independently read digest (`627c1b23854cde8d66ce41c0fc18a786e8973c430dccf7b23acd5c5f4a4e609c`). The null spelling and `str` collapse the B session saw were owned by the read window and delimited-null work that landed since. | Excel export writer + `services/dest_precount.py` | B | live `postgres_to_excel` cell, 100K rows, dest rows and checksum both matched |
| ~~D11~~ | **Closed.** The Avro export was not malformed Avro — it was **JSON**. `write_destination_file` only re-encoded the row grid into the destination's container when `can_convert(source_format, target_format)` held, and a database source has no file format of its own, so `can_convert("postgresql", "avro")` was false and the export fell through to the trailing JSON branch: 29 MB of a JSON array landed under an `.avro` name, the run reported success, and every Avro reader refused it (`read length must be non-negative or -1`). The same fall-through silently applied to **orc** and **xml** from every database source — the connector-family sweep, not just the reported cell — while `excel`/`parquet` escaped it only because they had their own explicit branches. A container is now decided by the *destination* alone (and an empty population is still that container, not an empty JSON array), and a format the exporter cannot write refuses instead of landing JSON bytes under its name. | `src/transfer/adapters.py` (`write_destination_file`) | B | 16 cases in `tests/test_file_export_container_formats.py` (container magic bytes per format, empty population, Avro record-for-record read-back, refusal for an unwritable format, text writers untouched), plus a live 100,000-row run on the compose Postgres: `avro`, `orc`, `xml`, `excel`, `parquet`, `csv` all `pass` with independent read-back of 100,000 rows and a checksum equal to the source population's (`fastavro` reads 100,000 records off the landed `.avro`) |
| ~~D12~~ | **Closed.** The grading, not the refusal, was the defect — and it was worse than "a refusal reported as a pass". Three faults, each of which could report a green cell for a product that did nothing right: (1) a refused cell was scored `pass` in the same token as a cell that moved 100,000 rows and checksummed them, so a matrix summary claiming *N passes* silently counted populations that were never written; (2) the score was `not success and destination COUNT(*) = 0`, which is equally what a dead Postgres, a wrong password or a mistyped fixture path looks like — an outage graded as proof of the ragged-row contract; and (3) the dirt sits at fixed row indices (a ragged row every 25,000, the non-numeric `qty` cell every 11,000), so any `--rows` below those wrote a *clean* file and the cell then graded the engine's ordinary, correct behaviour on clean data as `fail: engine accepted a ragged row` — a false accusation against the product, reproduced at `--rows 2000`. A refused cell now scores under its own token (`pass (refused)`, counted apart in both the harness tally and the generated report), only when the engine's own words name the refusal that cell provokes, and a population too small to carry the dirt skips with the row count it would have needed instead of returning a verdict. | `tests/scale/file_matrix.py` (grading) + `tests/scale/report.py` + `tests/scale/dirty_fixture.py` | B | 5 cases in `tests/test_scale_refusal_grading.py` (own token, outage/empty-error rejected, acceptance and partial write still fail, dirt gate, report tally), plus a live 25,000-row run on the compose Postgres: `csv_ragged` refused with *"CSV row has 13 value-bearing cells but the header names 11 column(s); refuse silent column drop"*, `strict_csv` / `strict_parquet` refused with *"qty (row 11000): Null sentinel 'N/A' cannot coerce to integer"*, `unsupported_xls` / `unsupported_zip` refused as unreadable carriers — all five with independent `COUNT(*) = 0`, and the same two cells at 2,000 rows skipping rather than accusing |
| ~~D13~~ | **Closed.** `FLUSH TABLES WITH READ LOCK` freezes every write on the instance, so a lock that outlives the coordinate capture makes a `mysql → mysql` route whose destination shares the server wait on its own snapshot until `lock_wait_timeout` (`1205`). Track D's minimal locking released it on the success path only; three paths still carried it past that point: (a) when pinning the read view or reading the coordinates raised, the lock connection was dropped **by reference** — the server frees a session's locks when the socket closes, so release was a refcounting accident, now an explicit `UNLOCK TABLES` + `close()` in a `finally`; (b) the per-table `LOCK TABLES … READ` fallback (no `RELOAD` grant) still believed it held locks MySQL had already released at `START TRANSACTION`, so the later `UNLOCK TABLES` **committed the read view away mid-dump**; (c) a read view pinned but coordinates recaptured afterwards were paired anyway, and that position is *later* than the view, so a change in between was neither dumped nor replayed — a silent gap, now resolved by giving the view up and dumping live. | `connectors/mysql_change_stream.py` | D | 6 cases in `tests/test_mysql_cdc_snapshot_lock_release.py` (4 fail on the previous code) + live on the compose MySQL in `tests/test_cdc_mysql_to_mysql_same_instance.py`: a 5,000-row `mysql → mysql` CDC route on **one instance** with an independent `COUNT(*) = 5,000`, a third session writing throughout the snapshot never refused with worst wait < 2s at `lock_wait_timeout = 5`, and the route still completing when the read view cannot be pinned. Caveat: the fault-injected case also survives on the base commit, because CPython's refcounting happened to close the socket — the fix makes the release a decision instead of luck |
| ~~D14~~ | **Closed by live re-proof.** The merge's canonical `bson_safe_document` does hold: a signed contract carrying `Decimal` scalars, a nested profiling map and a list lands in the compose MongoDB and reads back as exact `Decimal128` off a client the product never touched, and a decimal wider than the 34-digit BSON wire keeps its exact digits as text rather than being rounded into it (verified: `Decimal('1.9…')` at 61 digits stores as text, not a rounded `Decimal128`). The remaining half of the defect — "fell back **silently**" — is fixed too: a failed Mongo write said only `Exception suppressed`, so a control plane serving contracts one version behind its own store looked healthy; it now names the contract, the reason, and that the file mirror is answering. | `services/contract_store.py` + `services/value_serializer.py` | C | 2 live cases in `tests/test_contract_store_mongo_live.py` (independent client read-back) + the 66 existing contract/serializer cases |
| ~~D15~~ | **Closed.** The two base failures asserted the *old* contract, not a defect: a fractional cell an integer column refuses is now widened to exact `DECIMAL(8,6)` rather than `DOUBLE` (an IEEE binary carrier cannot hold `22.433332`, which is the reason the row was held out), and a writer binds a mapped decimal as `Decimal`, so the quarantine proof asserts decimal identity *and* its text (`"10.50"`, scale intact) instead of text that happened to match. Widening the selection then exposed a real defect that reached every SaaS destination: `requests` encodes only JSON-native types, so any reverse-ETL batch holding a mapped `DECIMAL` (or a temporal, UUID or bytes cell) died with `Object of type Decimal is not JSON serializable` — the whole chunk, not the cell — on Salesforce, HubSpot, Airtable, Zendesk, Stripe, Shopify and Notion alike, since all of them post through one shared request. That request now sanitizes the body once before `requests` sees it: a decimal stays exact text (never a binary float writing `500.25000000000002784` into a customer's revenue field) and a non-finite or absent value is refused instead of wiring `NaN` or the `__DF_MISSING__` sentinel into a CRM record. Two proofs were also fixed to prove the product rather than their own stubs: the MySQL hostile-identifier test answered the table-existence probe with a row (`(0,)`), so the writer correctly refused the empty DDL read and never built the INSERT under inspection (the stub now answers as MySQL does, and closes); and the Salesforce activation route left Describe to the network, where a proxy answering 401 reads as an auth failure and the write is refused — Describe is now stubbed. | `connectors/saas_common.py` + the quarantine/proof expectations | base | 8 new cases in `tests/test_saas_request_body_serialization.py` (exact decimal, temporal/UUID/bytes, JSON-native untouched, non-finite and sentinel refusal), plus `test_salesforce_live_transfer` (13, live HTTP round-trip, upsert re-run does not duplicate and `AnnualRevenue` lands `500.25`), `test_security_hardening_e2e` (7), `test_e2e_market_features` (8) and the 12 writer-fit/resilience cases |
| ~~D17~~ | **Closed.** 14 typed-transfer tests failed on the merged base, before any of this session's work (identical set reproduced on a clean `bc654de3` worktree): `test_execute_tracked_*` type-preservation for `csv/tsv/json/jsonl/parquet → duckdb`, `duckdb → duckdb`, `duckdb → postgresql`, `json/jsonl → postgresql`, edge-types `csv → postgresql`, `test_gate8_bind_fingerprint_and_saas_typed::test_saas_catalog_stays_planned_not_sku`, and two cells of `test_typed_fidelity_transfer_matrix_e2e` (`mongodb → postgresql` refused by *Mapping confidence below floor*, `postgresql → existing mysql timestamp column`). Five distinct causes, each fixed at its owner: (1) a percent/currency/accounting marker was judged *before* normalization, so `0%` read as a leading-zero identifier and the column was diverted to `TEXT`; (2) DuckDB routes hard-coded `/tmp`, unopenable on Windows; (3) JSON-family `""` is a real empty string while a blank CSV cell is NULL — expectations restated to that rule; (4) MongoDB `_id` was folded onto `id`, and an identity text-family mapping acquired a `trim_id` transform that pushed it under the Map confidence floor (floor unchanged, the mutation removed); (5) an overwrite/full-refresh run bound the *stale* existing destination type (`VARCHAR(64)`) although the table is dropped and recreated from the source — recreation truth (`dest_recreated`) now flows through the destination probe, schema drift, G3/G6/G9, G14 and G15, while append/upsert/CDC keep the live schema authoritative and still refuse narrowing. | `services/transform_engine.py`, `services/schema_inference.py`, `services/semantic_mapper.py`, `services/mapping_pipeline.py`, `services/schema_drift.py`, `services/preflight_service.py`, `services/shape_contract.py`, `services/destination_requirements_gate.py`, `src/transfer/engine.py`, `packages/preflight/.../gates.py` | base | live PostgreSQL → existing MySQL overwrite: dest recreated as `LONGTEXT`, stale row gone, `COUNT(*) = 2`, values re-read; the same route as `incremental_append` still blocks (G3/G6/G9/drift) with dest `COUNT(*) = 0` and `varchar(64)` untouched; `test_typed_fidelity_transfer_matrix_e2e` 12 passed / 5 skipped; 1,223 passed in the preflight/shape/drift/mapping/sync sweep |
| ~~D18~~ | **Closed.** Two defects wore one symptom. (1) A local warehouse was handed to pyiceberg verbatim, so `C:\wh` was read scheme-first and refused as filesystem `c`; local warehouses are now resolved to an explicit `file://` URI (`local_path_from_location`, which also inverts `file:///C:/wh` back to `C:\wh` rather than the unopenable `\C:\wh`), while an object-store or remote warehouse — `s3/s3a/gs/abfs(s)/wasb(s)/hdfs/oss/http(s)/arn` — is preserved byte-for-byte and never turned into a local directory. PyArrow then receives the URI path `/C:/wh`, which Windows also refuses, so a local-URI `FileIO` strips the drive-rooting slash before the filesystem sees it. (2) The far worse one, only visible once the catalog worked: the filesystem writer reported the *warehouse directory* as `target_schema`, so reconciliation re-resolved `root / <absolute table dir> / table`, read an empty destination and reported every correctly written key as `MISSING_TARGET` — a clean write proved as data loss. Both writers now report the logical namespace, and the table identifier is kept separate for messages. | `connectors/iceberg_catalog.py`, `connectors/iceberg_pyarrow_io.py` (new), `connectors/iceberg_writer.py`, `services/dest_precount.py` | base | 12 new cases in `tests/test_iceberg_windows_warehouse_paths.py` (URI round-trip both directions, remote-scheme preservation, FileIO binding, namespace reporting) + the 5 originally failing cases now passing, and 246 passed / 4 skipped across `test_row_conservation.py` + `test_property9_row_conservation.py` (sole remaining failure `test_bigquery_dest_count_queries_not_table_metadata` is a scripted-client stub that rejects `retry=`, unrelated and present on base) |
| ~~D19~~ | **Closed.** The cause was not a gate that failed to fire but one that was correctly told the question no longer existed: `full_refresh_overwrite` drops and recreates the destination, so D17 made the live destination type non-authoritative (`dest_recreated`) for G3/G6/G9/G14/G15 — and with the declared `amt_dec INTEGER` cleared, nothing was left to narrow against, so all 200 rows landed into a column silently recreated `NUMERIC(20,9)`. Append, upsert and CDC refuse that same pair. The recreate semantic stays (it is what makes a second overwrite tick reproducible); what was missing is that the *replacement itself* was never stated. New gate **G19** keeps the doomed schema on the destination probe (`overwrite_replaced_column_types`), and blocks a recreate that would discard a declared carrier the source overflows — deliberately narrower than `is_lossy_coercion`, which also answers fidelity/domain questions (`CHAR(36) → UUID`) that on a recreate are usually Datawrap arguing with a carrier Datawrap created on the previous tick. Widening and identical recreates pass; a continue-policy Migration Risk Contract demotes the block to a warning and records the replacement in the proof pack. | new `services/dest_schema_replacement.py` + `services/preflight_service.py` + `src/transfer/engine.py` (destination probe) | base | 14 cases in `tests/test_dest_schema_replacement.py` + live compose Postgres: destination `amt_dec integer` before, run refused (*"amt_dec (INT4 → NUMERIC(20,9), source DECIMAL(20,9))"*), destination still `integer` and `COUNT(*) = 0` on an independent psycopg2 connection. `run_matrix --engines postgresql,mysql,sqlite`: the 2 `dest_exists_narrower` cells that fail on base now pass; the 3 remaining `mysql → sqlite` fidelity failures reproduce identically on base |
| ~~D20~~ | **Closed.** D13 (c) removed the *recapture* gap on the global-lock path but left the ordering that creates it: `snapshot()` pinned the read view first and read `SHOW MASTER STATUS` afterwards. MySQL releases `LOCK TABLES … READ` the instant a transaction begins, so on the no-`RELOAD` fallback the table locks are gone before the coordinates are read — a commit landing in that window is behind the replay position and ahead of the read view, dumped by neither phase. Coordinates are now captured **while the lock still holds**, and only then is the repeatable-read view pinned: the position is at or before the view, so the overlap is replayed (at-least-once, absorbed by upsert) instead of a silent gap. Lock lifecycle, the release-on-every-error-path guarantee and the dump-without-lock behaviour are unchanged. | `connectors/mysql_change_stream.py` | Devin Review on #124 | 8 cases in `tests/test_mysql_cdc_snapshot_lock_release.py` (2 new, both fail on the merged code) + live on the compose MySQL: `tests/test_cdc_mysql_fallback_coordinate_window.py` withdraws `RELOAD`, commits a row from a third session exactly at the coordinate capture, and reads it back on an independent pymysql connection — **fails on the merged connector** (row in neither dump nor stream), passes after. `tests/test_cdc_mysql_to_mysql_same_instance.py` still green (11 passed, same-instance route, concurrent writer, unpinnable read view) |
| ~~D16~~ | **Closed.** Two unrelated items under one number. (a) The CI mypy selection reported 6 errors, not 4: five `Returning Any` from helpers whose callee is untyped, and one missing return annotation on `execute_gate._rebuild`; annotated at the call sites, no behaviour touched (`Success: no issues found in 17 source files`). (b) The mapper was never the problem — its *input* was. `read_index_batch` reported no `native_types`, so `_schema_from_batch` fell back to the bare `string` placeholder it uses for sources with no catalog, and the whole Elasticsearch destination read back as text. An index **declares** its fields, so Map was demoting `id → id` (source `INTEGER`, declared destination `long`) to **0.63** and holding an exact-name identity pair for review on a route that is not lossy. The reader now reports the index mapping as the batch's native types, reusing the writer's mapping reader (`ObjectApiResponse` body, alias→concrete index, `meta.df_carrier` outranking the field type since `scaled_float` cannot express `DECIMAL(12,4)`), on the first page only and never for fields the mapping leaves dynamic — those keep the placeholder rather than acquire an invented carrier. | `connectors/elasticsearch_reader.py`, `services/type_system.py`, `services/decision_kernel/findings.py`, `services/decision_kernel/execute_gate.py` | index, E | 9 cases in `tests/test_elasticsearch_declared_types_stamping.py`, incl. the pipeline pair pinned at 0.63 (placeholder) vs 0.99 (declared) and a **live** local Elasticsearch index read through the product's own `introspect_endpoint` (`long → BIGINT`, `scaled_float+meta → DECIMAL(12,2)`, `id → id` at 0.99); 331 passed / 30 skipped across the elasticsearch, mapping-pipeline, semantic-mapper and endpoint-intelligence suites |

### Closure PRs (2026-08-30 wave)

| Defect | PR | One-line closure |
|--------|----|------------------|
| G16 Field Reduction Ledger (feature, not a defect) | [#125](https://github.com/Venky-Chowdary/DataFlow/pull/125) | typed disposition + reason/approver per dropped source field, fail-closed gate, ledger in the signed proof pack, Map reduction-reason control |
| Durable evidence (feature) | [#126](https://github.com/Venky-Chowdary/DataFlow/pull/126) | hash-chained append-only evidence with a signed per-job head and a standalone re-walk verifier, replacing the bounded in-memory deque |
| D19 | [#127](https://github.com/Venky-Chowdary/DataFlow/pull/127) | G19: a full refresh may recreate a destination, but not silently redefine a declared carrier the source overflows |
| D20 | [#128](https://github.com/Venky-Chowdary/DataFlow/pull/128) | MySQL CDC fallback: binlog coordinates captured while the lock still holds, closing the silent snapshot/stream gap |
| D18 | [#129](https://github.com/Venky-Chowdary/DataFlow/pull/129) | Iceberg local warehouses resolve as `file://` URIs; writers report the logical namespace, not the warehouse directory |
| D16 | [#130](https://github.com/Venky-Chowdary/DataFlow/pull/130) | mypy baseline clean; the ES reader reports the index's declared mapping types, so `id → id` is 0.99 instead of 0.63 |
| D1 | [#132](https://github.com/Venky-Chowdary/DataFlow/pull/132) | sampled dest carriers are a profile that widens; declared catalogs stay enforced; NoSuchKey stays unread |

This sequence is closed. §2 / §3 items are not defects and are not counted green.

## 2. Not measured (product may be correct; no proof exists)

1. 100K on every route; 1M on every sync mode (1M measured on exactly one route:
   PostgreSQL→MySQL append, 221.5 s / 4,515 rows/s).
2. ~~Track A's 225-cell grid never completed — the re-run halted at 122 cells.~~
   **Closed for the live 4-engine 200-row grid (this PR).** 16 routes × 10
   shapes = 160 cells: **140 pass / 0 fail / 20 skip** (prefix `ta5`,
   2026-09-01). Oracle `:1521` down — 0 Oracle cells; not 225/225. Skips are
   honest: 16 `domain_contract_unsigned` (dest already declares the domain) and
   4 `* → sqlite` `dest_exists_narrower` (SQLite does not enforce string
   length). Evidence `docs/SCALE_MATRIX_SQL.md` addendum + harness JSON.
   **100K on this grid is still not measured.**
3. `postgresql→mongodb` CDC at 100K: timestamp risk-acknowledgement not re-measured.
4. ~~Scheduler DST cell and workspace-ownership cell not re-measured after the
   `workspace_access` fix~~ **Closed (this PR).** Track D cadence cells
   (interval, tz cron, DST boundary: offset re-derived, spring-forward 02:30
   skipped, fall-back 01:30 `fold=0`) pass against independent `zoneinfo`
   arithmetic. Same actor in two workspaces: `X-Workspace-Id` of the sibling
   404s an id-addressed GET and omits the schedule from the list; a non-member
   read/create is 403/404. Proven:
   `tests/test_scheduler_dst_workspace_remeasure.py` (4 passed) plus the
   existing isolation suite (3 passed). The 100K scheduler *beat* was not
   re-run.
5. `mongodb→mysql` CDC idle re-run; the 100K crash-injection pass.
6. BigQuery-emulator cells beyond the two-run identity probe.
7. Full-fleet NoSQL sweep on the fixed revision (last complete sweep: 73 pass /
   24 fail / 24 skip at 200 rows, on a *pre-fix* revision).
8. ~~`fixed_width` and `yaml` as live file drivers~~ **Closed as sources
   ([#136](https://github.com/Venky-Chowdary/DataFlow/pull/136)) and as 100K
   Postgres sources ([#137](https://github.com/Venky-Chowdary/DataFlow/pull/137)).
   YAML **dest export** is closed (this PR): `dump_yaml_records` writes a
   sequence of flat mappings with every scalar double-quoted (YAML 1.1 cannot
   coerce `yes`/`NO`/`007`), empty population is `[]`, dest COUNT is
   `iter_yaml_dicts` on disk. Fixed-width dest export is still refused (needs
   a declared layout). 100K MySQL twins were not run (`mysql_up()` false).
   YAML dest 100K was not measured.
9. Real SMTP / Slack / Teams delivery; MCP from a real client; chatbot/RAG
   against a live key; real host routing per client domain; SSO/IdP; KMS/BYOK.
10. CDC is **at-least-once** everywhere except the named crash-injection routes.

## 3. Environment (cannot be proven on this box)

AWS S3, real GCS, real ADLS, hosted BigQuery, Snowflake, Redshift, Databricks,
Salesforce / HubSpot / Airtable / Kafka (no credentials); Iceberg (no REST
catalog); Elasticsearch security API absent from the local image, so the
connector's privilege preflight fails closed; ClickHouse capability is `Planned`,
so the product refuses it by design.

---

## 4. The fleet a closure is proved against

`python scripts/local_engines.py` starts the engines `docker-compose.yml`
declares (Postgres, MySQL with binlog + CDC grants, Mongo as a replica set,
Redis; `--with-search` adds Elasticsearch) and prints the exact environment the
live suites read. That last half matters: live suites resolve credentials
through `apps/api/tests/helpers/live_env.py`, which reads `PGHOST`/`PGUSER`/…
and `MYSQL_*` before a default that does **not** match this repository's compose
file — so a healthy fleet still produced dozens of silent `skip`s. Export what
the script prints, or a live matrix will grade itself green while proving
nothing. `--check` exits non-zero when a declared port is unreachable.

## 5. Pre-handover gate wave (2026-09-06, PR [#168](https://github.com/Venky-Chowdary/DataFlow/pull/168))

Found by driving the real application, not by reading it. Every closure below was
proved on a live Postgres source and a live MySQL destination, with the
destination re-read on a `mysql` connection the transfer engine never touched.

| # | Defect | Owner module | Closure evidence |
|---|--------|--------------|------------------|
| ~~D21~~ | **Closed.** A declared G21 control total failed the whole job on a MySQL destination: the scan SQL was hard-coded PostgreSQL text casting (`CAST(SUM(col) AS TEXT)`), which MySQL rejects with error 1064. The control-total scan now goes through the destination-dialect helper the rest of the engine already uses, so MySQL reads `CAST(... AS CHAR)`, SQL Server `CONVERT(VARCHAR(64), ...)`, Oracle `TO_CHAR(...)`, and an unknown engine keeps the uncast expression so exact parsing refuses a float payload rather than certifying it. The executed scan expression is recorded in the evidence. | `services/control_totals.py`, `services/decimal_identity.py` | live PG→MySQL: `SUM(amount)` read back as exact `'618.75'` off an independent `mysql` connection, `1064` count 0 in the API log; a one-cent destination (`618.76`) still fails closed with both sums named |
| ~~D22~~ | **Closed.** G20 could never be *asked* for an ordinary code column. Map offered no way to declare a crosswalk for a plain character carrier (`VARCHAR(4)`), so codes landed untranslated and the gate reported the column as not asked. An eligible undeclared character column now carries an explicit `Declare code crosswalk` action. Eligibility is not a declaration: nothing is inferred from the type, `A → A` must still be entered explicitly, and an uncovered population value still blocks. | `apps/web/src/lib/mapping.ts`, `apps/web/src/components/ColumnReviewPanel.tsx` | live browser run: a partial mapping blocks naming both uncovered codes, a complete mapping passes with `(3 distinct value(s), population scan)`, translated values re-read from MySQL, and the declaration survives Map → Validate → Map |
| ~~D23~~ | **Closed.** A *proven* control total was invisible to the operator — it existed only in the exported pack. The panel had been added to `JobTheater`, which no successful Transfer Studio run mounts. It now lives on the Gate-8 card itself, the one card Transfer Studio, Jobs, Validate and the Theater all render. The evidence wording was also dishonest by accident: the engine writes `unmeasured` for any unproven run, mismatches included, so two cent-exact sums sat under a label that read as if nothing had been measured. Sums are carried as decimal strings end to end, never JavaScript numbers. | `apps/web/src/components/transfer/Gate8ProofCard.tsx`, `apps/web/src/lib/gate8Population.ts` | live browser run on the surface that was previously empty: `Proven` / `population SUM, exact` / `618.75` / `618.75` on both the Transfer Studio terminal and Jobs detail; the mismatch run reads `Mismatch` / `measured, sums disagree`; a run with no control total declared shows no panel at all |
| ~~D24~~ | **Closed.** The MySQL COPY fast paths raised `AttributeError: module 'os' has no attribute 'mkfifo'` out of the fast path on Windows instead of declining it, so the row-writer fallback never ran: the destination was created and left empty. The FIFO-streamed routes now check the precondition before any connection or DDL and decline as an unavailable fast path. Same-instance MySQL `INSERT...SELECT` is untouched, because it needs no named pipe. | `services/copy_fast_path.py`, `services/copy_pg_mysql.py`, `services/copy_mysql_pg.py`, `services/copy_mysql_mysql.py` | 5 passed / 1 skipped in `tests/test_copy_fifo_platform_guard.py`; live PG→MySQL declines at INFO and the row writer commits all 6 rows with Gate-8 count and checksum proof |

### Still open after this wave

| # | Item | Kind | What is known |
|---|------|------|---------------|
| — | The Gate-8 card on a **completed** Theater. | not measured | `JobTheater` renders it only on `isComplete && job.reconciliation`, and `handleJobComplete()` clears `activeJobId` immediately, so the state is transient by construction: a 250 ms polling observer caught the Theater host live before the surface swapped to the terminal dashboard. Either the surface stops being cleared, or it is documented as unreachable — it must not be left as an unverified surface. |
| — | Em-dash rendering for a missing control-total SUM. | not measured | No natural case arises on a SQL→SQL route: `control_totals.py` nulls a sum only when the SUM query itself fails. Unit-covered, not browser-proven. |
| — | Schedules, retries, overlap and DST; job cancellation; quarantine and replay; the Evidence Chain, Operations, Contracts and Proofs pages; workspace roles and member removal; G19 hard-block reachability; Mongo and MinIO routes. | not measured | Untouched by this wave. |

## 6. Handover sweep (2026-09-07, branch `devin/1788705057.72211-handover-gate-fixes`)

| # | Defect | Owner module | Closure evidence |
|---|--------|--------------|------------------|
| ~~D25~~ | **Closed in two parts.** (a) A freshly exported signed proof pack failed the product's own **verify** control (`content_sha256 mismatch; HMAC signature invalid; chain_anchor digest does not match`): the signature was computed over the in-memory objects the pack was built from, not over the bytes that were written, so any serialisation difference read as tampering. (b) Browser QA then failed the *same* three checks on the round trip an operator actually performs — export, download, re-upload — because JSON has one number type: the signer hashed an integral float as `100.0` and the browser handed the verifier back `100`. Hashing now normalises an integral float to its integer form before canonicalisation; non-integral floats and the decimal **strings** money travels as are untouched. | `services/signed_proof_pack.py` | commits `f16b853e`, `e33b3c64`; 79 passed across the proof-pack / audit-verify / ledger / certificate suites, incl. two cases that put the pack through an exact browser-style reparse and one that mutates a byte after the reparse and still fails closed. **Browser-proved 2026-09-07**: a pack exported from a completed job, downloaded (32,564 bytes) and re-uploaded byte-identical verifies — `HMAC proof pack verified · content 93831c0d3d78…`, `{"ok":true,"errors":[]}`; mutating one byte inside a signed value (`target_rows` 6 → 7, identical length, signature untouched) still fails all three checks, so the normalisation did not blind the verifier |
| ~~D26~~ | **Closed.** A destination carrier the operator picked by hand (widening `VARCHAR(4)` → `VARCHAR(255)`) reached Validate and was honoured by a direct Execute, but was replaced by the inferred type on returning to Map — the plan the operator reviewed was no longer the plan Execute would run. Map re-derives every row when the step or destination schema changes, and the decision replay carried reductions, crosswalks and control totals across that regeneration but not the carrier. `EditableMapping.destTypeDeclared` now records the choice, and it is replayed by source name *before* the decision fingerprint is read, so an approval signed for the declared carrier survives with it. A dropped column is not restored, and an inferred carrier with no declaration behind it is left alone. | `apps/web/src/lib/mapping.ts`, `apps/web/src/lib/mappingDecisions.ts` | commit `404ab1bc`; 18 passed in `mappingDecisions.test.ts` including four new cases, `tsc -b` clean. Browser-proved 2026-09-07: the declared `VARCHAR(255)` survives Map → Validate → Map and drives the DDL — independent MySQL reads `status varchar(255)`, 6 rows, `SUM(amount)` 618.75 |
| ~~D27/D28~~ | **Closed.** In the multi-table (stream-contract) route every fast-path `CREATE` ignored the carrier Map declared and fell back to `TEXT`, so an INTEGER/BIGINT key landed as SQLite `TEXT`; and a `dest_count:<n>` token was being read as a value-fidelity digest when it is cardinality proof only. | `apps/api/src/transfer/copy_route.py` | commit `db459d06` |
| ~~D29/D30~~ | **Closed.** The COPY fast paths declined on any host without `os.mkfifo` (superseding the D24 decline): the routes now overlap producer and consumer on a FIFO where the host has one and spill to a temporary file where it does not, preserving bytes, row order and cleanup, with `DATAFLOW_COPY_SPILL_DIR` for an operator-chosen volume. Separately, ODBC-only connect options (`TrustServerCertificate`, `Encrypt`, `ApplicationIntent`, …) were passed to `pymssql`, which rejects them; the URL vocabulary now follows the selected DBAPI and fails closed with a named remedy when FreeTDS cannot honour a requested option. | `services/copy_fast_path.py`, `services/copy_pg_mysql.py`, `services/copy_mysql_pg.py`, `services/copy_mysql_mysql.py`, `connectors/generic_sql.py` | commit `68852071`; 7 passed / 1 skipped in `tests/test_copy_fifo_platform_guard.py`, 11 passed in `tests/test_pgvector_pgvector_copy.py` |
| ~~D31~~ | **Closed.** The local CSV/YAML/fixed-width → SQLite COPY fast path bound every cell with `int()` / `float()` on the physical carrier, so a dirty cell (`false` in an INTEGER column) raised `ValueError` *out of* the fast path instead of declining it, and a grouped EU-locale money cell (`1,234` for `1.234`) landed verbatim because SQLite rematerialises DECIMAL as `TEXT` — the physical carrier alone cannot tell money from free text. The declared logical type now travels with the DDL into the binder: a non-canonical numeric cell raises `FastPathUnavailable`, and the row path owns locale parsing, transforms, validation and quarantine as it already did. | `services/copy_sqlite_common.py`, `services/copy_csv_local.py` | commit `42d0ab9f`; 12 passed in `tests/test_sqlite_copy_bind_declines.py`, 45 passed / 2 skipped across `test_number_locale_contract.py`, `test_yaml_fwf_scale.py`, `test_scale_transfer.py`, 191 passed across the copy/locale suites |
| ~~D32~~ | **Closed.** Every engine-side keyed upsert / incremental route failed Gate-8 while being correct. The writer proves cardinality by counting destination rows whose key is in the staging table and reports `pk_join_count:<n>` where a digest would go; reconciliation recognised only `dest_count:<n>`, so it re-read the destination, produced a hex digest, compared it to a row count and refused the run — under a message that blamed a missing reconcile sample while its own L4 layer reported the columns as matching. Both tokens are now graded as the cardinality proof they are, under either scope, and the evidence names which count carried the proof and states that value fidelity was not compared. A real mismatch still fails. | `apps/api/src/transfer/reconcile_step.py` | commits `5860ee89`, `ea5a94f9`; 5 passed in `tests/test_engine_streaming_sqlite.py`, 7 passed in `tests/test_fast_path_declared_carrier.py` |

### Browser-QA wave inside the same sweep (2026-09-07)

Found by driving the running application, page by page, not by reading it.

| # | Defect | Owner module | Closure evidence |
|---|--------|--------------|------------------|
| ~~D36~~ | **Closed.** The approval inbox rendered another tenant's parked schedule — its name, its finding, and both connector identifiers — to a workspace that does not own it. The route declared `workspace_id` as a plain argument, so FastAPI read it as a *query* parameter and ignored the `X-Workspace-Id` header every client actually sends; the scope resolved empty and the query returned every workspace's parked work. Execution had failed closed independently (an id-addressed run 404s), so this was disclosure rather than cross-tenant execution — which is still the half a security review reads first. The route now binds the header through the same `resolve_read_workspace` helper as every other scoped read. | `apps/api/src/routers/schedules_router.py` | commit `455f380d`; 24 passed in `tests/test_scheduler_dst_workspace_remeasure.py`, including a case that parks a schedule in one workspace and asserts the sibling's inbox is empty **and** that neither connector id appears in the response body |
| ~~D37~~ | **Closed for the refusal half; the positive half is unproven in the UI.** Promote / Replay was offered whenever the quarantine ledger held an open count. Findings that name no column and carry no stored cell values — integrity findings counted in that same total — have no payload for the writer to rewrite, so every click returned `400 Could not reconstruct rows from quarantine details`, and the message lived in a toast that faded: the control read as a button that does nothing. The panel now grades open findings for a rebuildable payload, disables replay when none has one and says what to do instead, a refused replay renders inline and stays there, and the API refusal names the reason rather than the internal step that produced it. | `apps/web/src/components/transfer/QuarantinePanel.tsx`, `apps/api/src/routers/connectors_router.py` | commit `b22316e6`; 3 passed in `quarantineReplayable.test.ts` (named cell, stored row, neither), `tsc -b` clean. Browser 2026-09-07: a no-payload finding correctly **disables** replay with the export-CSV explanation. The enabled path is **could-not-test** — no route through the UI reaches a payload-bearing *write-time* rejection, because Map's carrier list offers no narrow `DECIMAL` for a `DECIMAL(12,2)` source, and the `→ DATE` alternative dead-ends in D40. Fixing D40 is expected to unblock it |
| ~~D38~~ | **Closed and browser-proved.** Editing a schedule's destination kept the Decision Artifact hash Studio took against the *previous* one. That hash can never match again, so the cadence tick and the "Run now" the failure card offers as recovery both refused with `Decision Artifact content_hash mismatch — Map/DDL drifted since Validate`, and no control in the product could clear the stamp: the schedule was dead until someone deleted and rebuilt it. An edit that moves any field the stamp was hashed against (either endpoint, either table, the mappings, the stream contracts, the sync mode) now drops the artifact and DDL identity hashes, leaving the schedule exactly where one created without a Studio Validate already sits — the run stamps inline and the gates judge what is there now. A re-Validate that sends its own new hash keeps it; a rename or a cadence change does not touch the stamp. | `apps/api/services/schedule_store.py` | commit `8a29f451`; 4 new cases in `tests/test_schedule_stamp_route_edit.py`, 246 passed / 1 skipped across every schedule / scheduler / approval suite. Browser 2026-09-07: a destination edit dropped both stamps to `''`, `Run now` returned `200 success:true` and the job **completed** with no mismatch, independent MySQL read `qa_d38_d` = 6 rows / `SUM(amount)` 618.75, the cadence tick judged the current route (failing only on a genuine duplicate-key guard), and rename-only and cadence-only edits each preserved both hashes **byte-identically** |
| ~~D39~~ | **Closed.** Audit Logs → Verify chain reported 28 broken links and forks on a store nobody had tampered with. A timestamp is not an ordering: records written inside one clock tick — a burst of evidence writes during a single job — sort arbitrarily, so the tip query could hand the next append a predecessor that was not the last record written (a genuine fork), and a later re-walk could read a pair back in the opposite order (a broken link). Both readings were the product accusing its own evidence, which is worse than no verifier. Each record now carries a strictly increasing `chain_seq`, and the tip query, the audit list and the verifier all order by `(time, chain_seq)`. Records written earlier have no `chain_seq`, sort as `0` and keep their existing order. | `apps/api/services/audit_log.py`, `apps/api/services/evidence_chain.py` | commit `a3ac7f3a`; 3 cases in `tests/test_audit_chain_write_order.py` — all three **fail on the previous code** — plus 178 passed / 1 skipped across the audit, evidence, chain and proof-pack suites. Repairs chains written from here on: findings already recorded against an existing store stay as they are, because un-crossing them would mean rewriting audit history. Browser 2026-09-07: 22/22 records written after the fix went live carry a monotonic `chain_seq` (1→22) and **no finding lands on any `chain_seq`-bearing record**; the screen still reads `Chain verification failed — 36 record(s)`, every one of them a pre-fix `chain_seq: None` record (8 of them minted during the QA session itself by a still-running pre-fix API process). Two things stay unproven: none of the 22 post-fix records shared a timestamp, so **the tie-break the fix exists for was never exercised**, and the client-facing screen still reads as a failed chain until the legacy records are checkpointed or annotated |

### Still open after this sweep

| # | Item | Kind | What is known |
|---|------|------|---------------|
| ~~D40~~ | **Closed and browser-proved** — see §8. | closed | PR [#171](https://github.com/Venky-Chowdary/DataFlow/pull/171) |
| D33 | An engine-side keyed upsert reports no row-accounting census: `inserts` / `updates` / `deletes` are `None` in the destination summary, so the ledger cannot say how the destination population changed even though its cardinality is proved. | open defect | Surfaced by `tests/test_stream_append_precount.py` and the `target_rows_before` failures; the writer knows the pre-count and the join count and can derive it. |
| D34 | `kafka-python` is not installed in this environment, so every `*_to_database_kafka` cell of the live matrix errors instead of skipping. | environment / dependency | 47 matrix cells; the routes are unmeasured, not proven broken. |
| D35 | The local Qdrant is reachable from Windows on 6333 but the matrix cells fail with `HTTPConnectionError`, i.e. the suite resolves a different host/port than the forwarded one. | environment / fixture | 24 matrix cells unmeasured for the same reason. |
| — | Browser re-proof of D26 (carrier round trip) and D31 (EU-locale money), and the whole of §5's untested-surface row. | not measured | The native browser tool failed to initialise on four consecutive attempts in this sweep, so no UI evidence was produced. Recorded as untested, not as passing. |

### Full-suite state at the end of this sweep

A 115-file reconciliation/transfer collection run against the live fleet reports
`210 failed, 2240 passed, 296 skipped`. Classified: **131** are SaaS
connectors refusing a write by design or having no sandbox credentials, **71**
are the Kafka/Qdrant matrix cells above (D34/D35), **3** are D33, **1** is a
streaming reconcile-sample case, and the remainder are SQL Server ODBC/TLS,
Oracle credentials, Windows file-lock teardown and SQLAlchemy fixture problems.
Starting the fuller engine fleet *raised* the failure count on purpose: cells
that used to skip silently now attempt a real route and report what they hit.
None of the 210 is a regression introduced by this sweep.

## 7. D40 closure and what it opened (2026-09-07, PR [#171](https://github.com/Venky-Chowdary/DataFlow/pull/171))

| # | Defect | Owner module | Closure evidence |
|---|--------|--------------|------------------|
| ~~D40~~ | **Closed in two halves, because the dead end had two causes.** (a) Map never asked for a contract on the routes the engine actually refuses: the frontend graded carrier *domains* (`text → decimal` looked like a plain domain change) while the backend `is_lossy_coercion` refuses on carrier *shape* — width, scale, national/blank-padded character, timezone awareness, binary→text, document specialties. So `TEXT → DECIMAL(38,15)` and `DECIMAL(12,2) → DATE` were blocked downstream while Map showed "Ready" with no policy selector and no signing control. Map now classifies by shape, renders a per-row execution-policy selector with **no hidden default** (Sign is disabled until a policy is chosen) and refuses a signature with no policy. (b) The signed contract then stopped working one screen later: G9's financial-precision check appended *every* unparseable value to `issues`, so a signed `QUARANTINE_ROW` on `amount TEXT → NUMERIC` still collapsed into `rc-fidelity-collapse-*` and Execute stayed locked — the operator was told to sign the contract Validate then ignored. A continue policy names the write-time disposition of a row the destination will reject, so that rejection is now a **contracted holdout** reported as a warning with the offending values. A silent magnitude shift has no rejection behind it and keeps blocking whatever the contract says. Fail-closed is unchanged: no contract, boolean acknowledgement only, `FAIL_JOB` / `STOP_TABLE` / `ABORT_TRANSACTION` / `RETRY`, and a tampered signature all still block. | `apps/web/src/lib/typeCarrierFidelity.ts`, `apps/web/src/lib/mapping.ts`, `apps/web/src/lib/mapBlockers.ts`, `apps/web/src/components/ColumnReviewPanel.tsx`, `apps/api/services/data_integrity.py` | commits `9d93aa14`, `9fde768d`; `scripts/carrier_parity_audit.py` compares Map's verdict to the engine's over an **812-pair** matrix on PostgreSQL / MySQL / SQL Server / Snowflake / BigQuery with **zero** engine-lossy-but-Map-safe dead ends; 46 passed in `tests/test_data_integrity.py` incl. a parametrised continue-policy matrix and six fail-closed cases (no contract, boolean ack, `FAIL_JOB`, `STOP_TABLE`, `ABORT_TRANSACTION`, tampered). **Browser-proved 2026-09-07** on a fresh API (13:12:19) and Vite (13:12:28) both postdating the change (12:58:48): on `amount TEXT → numeric(12,2)` the no-policy state disables Sign (`Choose an execution policy first — no hidden defaults`), `FAIL_JOB`/`STOP_TABLE`/`ABORT_TRANSACTION` each keep Validate blocked naming the policy and the remedy, and `QUARANTINE_ROW` releases **both** Map and Validate with `not-a-number` reported as a contracted holdout. Job `6a9ebb31d9b32cdb5e791716` ran to `Completed with quarantine` — 2 appended, writer ack 2, 1 held out — and independent psycopg2 reads (a connection the engine never touched) measured `count(*) = 2`, `SUM(amount) = 30.50` exactly, no NULLs, no duplicate ids. `DECIMAL(12,2) → DATE` renders the same risk surface and the same `contract · FAIL_JOB · blocked` refusal; it was deliberately not driven to Execute, since a currency figure in a `DATE` column is nonsense data. Caveat: the Postgres destination dropdown offers `NUMERIC`, not a literal `DECIMAL(38,15)`, so that exact spelling was proved by the parity audit rather than end-to-end |
| ~~D37~~ | **Positive half now proved, by D40's closure.** The refusal half closed earlier (§6); the enabled Replay path had never been reachable because no UI route reached a payload-bearing *write-time* rejection. | `apps/web/src/components/transfer/QuarantinePanel.tsx` | Browser 2026-09-07: the quarantine finding carries a real payload — `source row 2 · column amount · target amount · value not-a-number · reason Invalid currency: 'not-a-number' · policy quarantine`. Replaying it **unchanged** is refused inline and durably (`Wrote 0 row(s) · 1 still rejected · Gate-8: failed (post_write_failed) · Ledger diverging (1 open)`) and the destination stayed at 2 rows / 30.50. After editing the finding to `25.75` in the UI, Replay succeeded (`1 dest Δ this run (dest 2 → 3) — Gate-8 batch verified`) and an independent read measured `count(*) = 3`, `SUM(amount) = 56.25` exactly, `id = 2 → 25.75`, no duplicate ids — a keyed upsert, not a duplicate insert, with exact decimals and no float drift |

### Opened by this closure — both open, neither introduced by it

| # | Item | Kind | What is known |
|---|------|------|---------------|
| ~~D41~~ | **A non-castable value is written anyway on a SQLite destination.** `not-a-number` into a column declared `decimal` lands as row 3 of 3: nothing is rejected, `rejected_rows` is `0`, and `strict` / `maximum` validation modes do **not** fail — so the mode that exists to refuse silent damage does not refuse it. Four cases in `tests/test_data_integrity_p0.py` fail identically on merged base `66dadc93` and on this branch, so this predates D40 and is not a regression from it. It is why every earlier QA pass failed to reach quarantine on a file/SQLite route. | **closed — §8** | Reproduced on base and branch. Register originally guessed the SQLite write path (`connectors/sqlite_writer.py` value-fit scan and the declared-vs-affinity carrier it grades against). PostgreSQL rejects the same value correctly, which is how D40 was proved, so the fault is per-engine, not in the quarantine machinery. |
| ~~D42~~ | **The destination-side DLQ write fails, so quarantine evidence is control-plane only.** After a quarantining run the UI warns that the dead-letter write into the live Postgres destination failed: the destination table has none of `_df_qid`, `_df_job_id`, `_df_row`, `_df_column`, `_df_target`, `_df_value`, `_df_reason`, `_df_policy`, `_df_payload`, `_df_created_at`, `_df_promoted_at`. The finding *is* durable on the control plane (it was inspected and replayed), but there is no destination-side SQL record of the held-out row, and a client running this flow sees the same warning. | open defect / product decision | **closed — §8a**. The writer already auto-provisions the `_df_*` table; the failure was that the DLQ clone inherited the destination's procedure / dest-DML `extra`, so the `_df_*` rows were bound into the client's own INSERT/CALL. |
| — | Proof-pack export → download → re-upload on tip `9fde768d`; the *silent magnitude shift still blocks with a contract* guard. | not measured | The round trip was proved on an earlier tip (D25), not this one. The magnitude-shift guard is covered in code and unit tests but the browser fixture's bad value is non-castable text, not an in-range value that loses magnitude, so that specific guard was not exercised in the UI. |

## 8. D41 closure (2026-08-10, branch `devin/qa-lead-integration`)

| # | Root cause (measured) | Owner module | Closure evidence |
|---|-----------------------|--------------|------------------|
| ~~D41~~ | **Two fast paths wrote the bad cell, not the writer.** The register's guess (`connectors/sqlite_writer.py`) was wrong — that writer rejects `not-a-number` correctly. (a) SQLite→SQLite identity COPY is `INSERT … SELECT` after `ATTACH`; SQLite does not enforce a column's declared type, so a `NUMERIC`/`INTEGER` column that stores text moved verbatim and dest `COUNT(*) == source COUNT(*)` passed as proof. Direct repro: dest row `(2, 'not-a-number', typeof='text')`, `rejected_rows 0`. (b) CSV→SQLite local COPY read the carrier from `item["type"] or schema[...]` and ignored `target_type` — a second, weaker copy of the `declared_copy_carrier` owner (D27/D28's fix) — so `age → integer` was bound as `TEXT` and `not-a-number` landed. | `services/copy_sqlite_common.py` (`sqlite_source_carrier_census`, one owner, called by all 8 SQLite-source identity COPY paths: sqlite, pg, mysql, mongo, oracle, sqlserver, s3, iceberg, incl. the incremental `WHERE` population); `services/copy_csv_local.py::_pairs_and_ddls` now reads `declared_copy_carrier` | Census runs engine-side: one `COUNT(*)`/`MIN()` per numeric or boolean column over the exact population the COPY would move (`typeof()` for INTEGER/REAL/BOOLEAN, a deterministic `df_canonical_decimal` UDF for DECIMAL) and raises `FastPathUnavailable` naming column, count and a sample **before** the destination is created, so the row path — which owns validation policy — sees the population: strict/maximum fail, balanced quarantines with structured details. `tests/test_data_integrity_p0.py` 12/12 (was 4 failed); new `tests/test_sqlite_source_carrier_census.py` 29 passed (integer/real/decimal/boolean/text carriers, NULLs, incremental predicate scope, no-dest-created-on-decline, clean population still COPYs). Blast radius: `test_copy_csv_local`, `test_copy_fast_path`, `test_copy_incremental`, all `test_*_sqlite_copy`, `test_sqlite_copy_bind_declines`, `test_fast_path_declared_carrier` → 218 passed / 19 skipped (skips = live services absent) / 0 failed. Ruff clean; mypy adds 0 errors in touched files. |

## 8a. D42 + CSV bulk-COPY integrity closure (2026-08-10, `822ef11e`, branch `devin/qa-lead-integration`)

| # | Root cause (measured) | Owner module | Closure evidence |
|---|-----------------------|--------------|------------------|
| ~~D42~~ | `services/dest_quarantine.dlq_endpoint` cloned the destination with `extra` intact. A destination configured as procedure / dest-DML / before-after hooks (`dest_write_mode`, `dest_query_sql`, `dest_procedure_*`) therefore drove the DLQ rows through the client's INSERT or CALL — which has none of `_df_qid … _df_promoted_at` — and `*_df_quarantine` was never created. Reproduced live on PostgreSQL: `write_dest_quarantine` returned `ok=False, wrote 0 of 1`, user table untouched, no DLQ table. | `services/procedure_destination.DEST_PROCEDURE_EXTRA_KEYS` (the one list `plan_dest_procedure` reads) is stripped from the clone in `dlq_endpoint`; the primary destination keeps its contract. | Same fixture after fix: `ok=True, rows_written=1`, `*_df_quarantine` holds `('age','x')`, user table still empty. `tests/test_dest_quarantine.py::test_dlq_endpoint_is_a_plain_table_even_for_procedure_destinations`. |
| new | **CSV/TSV → PostgreSQL / MySQL / SQLite identity COPY could not quarantine at all.** `COPY FROM STDIN` / `LOAD DATA` parse the file all-or-nothing: `id,age / 2,not-a-number` with `age → integer` failed with PostgreSQL's own `invalid input syntax for type bigint`, `rejected_rows=None`, no `dest_quarantine`, no destination table — the row path never saw the row. Same class as D41 (fast path bypassing validation), file side. | `services/copy_fast_path.text_cell_copy_safe` (integer / decimal / float / boolean / date / datetime / time carriers; text and unknown carriers pass through) is run over every cell by `services/copy_csv_local._write_mapped_csv` while the mapped file is produced — before the destination is touched. An unsafe cell raises `FastPathUnavailable`, the row path runs, and the existing engine-side quarantine + DLQ owns the finding. Incremental CSV routes receive the same `declared_types`. | Live CSV→PostgreSQL, balanced: `success=True`, `rows [(1,30),(3,42)]`, `rejected_rows=1`, DLQ `('2','age','not-a-number')`. Strict: `success=False`, error names `not-a-number` (not the engine's COPY error), 0 rows. Clean population: `copy_fast_path=used`, 3 rows. `tests/test_csv_copy_carrier_census.py` 27 passed. Blast radius (CSV COPY, fast-path carrier, DLQ, P0 integrity, SQLite census, procedure destination, file stream): 189 passed / 2 failed — both (`test_file_stream_path::test_stream_file_to_database_from_path`, `test_file_stream_skip_matrix::test_postgresql_file_stream_skips_matrix`) fail identically on `b3fec86b` before this change and stay open in §3. |

## 8b. CSV bulk-COPY physical-capacity closure (2026-08-10, branch `devin/qa-lead-integration`)

The two items §8a left unmeasured were both run live and one of them reproduced as a defect.

| # | Root cause (measured) | Owner module | Closure evidence |
|---|-----------------------|--------------|------------------|
| new | **A syntactically valid integer that does not fit the physical carrier still aborted the bulk load.** `id,age / 2,99999999999999999999` with `age → integer` passed the §8a lexical census (it *is* an integer) and PostgreSQL's `COPY` failed with `value "99999999999999999999" is out of range for type bigint` — `rejected_rows=None`, no DLQ, no table. MySQL's row path caught it, but the balanced run then failed Gate-8 with two opaque hashes: `map_rows_for_fingerprint` graded the held-out row against Map's bare logical `integer` stamp, which `integer_storage_bounds` deliberately reports as unbounded, so the source digest kept a row the writer had quarantined. | (a) `services/copy_csv_local._pairs_and_ddls` already produced the physical DDL; `_mapped_csv_file` / `_write_mapped_csv` now receive `physical_types` + `dest_db` on all four routes (SQLite / PostgreSQL / MySQL incremental, generic non-incremental) and `services/copy_fast_path.text_cell_copy_safe` grades each cell through the same owners the row path binds with — `connectors.writer_common.fits_integer` and `fits_decimal` / `parse_decimal_precision_scale` — so census and writer agree on what fits. (b) `connectors.writer_common.physical_integer_carrier` (reuses `type_system.ddl_type` + `integer_storage_bounds`, no second range table) maps a bare logical `integer` to the carrier the create-new writer materializes (BIGINT on PostgreSQL/MySQL/SQL Server, INTEGER on SQLite; unchanged for Snowflake/Oracle/MongoDB where the stamp is legitimately unbounded) before `map_rows_for_fingerprint` applies the write-quarantine matrix. | Live, same fixture: PostgreSQL balanced `success=True`, rows `[(1,30),(3,42)]`, `rejected_rows=1`, DLQ `('2','age','99999999999999999999')`, Gate-8 green; strict `success=False`, error `does not fit … BIGINT`, 0 rows, DLQ row persisted. MySQL balanced/strict: identical numbers via `LOAD DATA` route (the §8a "MySQL unmeasured" cell is now measured for both `not-a-number` and overflow). Bounded decimals: `0.016666668` into `NUMERIC(11,8)` / `DECIMAL(11,8)` declines; `0.01666666` retains COPY. `tests/test_csv_copy_carrier_census.py` 27 → 65 passed (physical integer/decimal grid, SQLite overflow decline, `physical_integer_carrier` per-engine grid, fingerprint holds out the same row as the writer, live PG + MySQL overflow balanced/strict with DLQ read-back). Blast radius (70 files: CSV COPY, fast path, DLQ, P0 integrity, SQLite census, procedure destination, file stream, every `*fingerprint*` / `*gate8*` / `*reconcile*` / `*integer*` / `*checksum*` suite): **503 passed / 4 failed / 1 skipped**; all 4 (`test_file_stream_path`, `test_file_stream_skip_matrix`, `test_informix_firebird_gate8_wave45::…no_log_and_null_safe_on` (`_Table` stub has no `.c`), `test_vector_gate8_wave38::…weaviate_and_milvus_route`) fail identically with this change stashed and stay open in §3. Ruff on the touched files: 29 findings before and after (all pre-existing BLE001 outside the CI allowlist); CI mypy allowlist clean; mypy on the 3 touched modules unchanged at 31 baseline errors. |

Full backend suite on `d693555f` (one clean detached run, all local services up, 1h19m): **19977 passed / 152 failed / 1105 skipped / 1 error**. None of the 152 is introduced by §8b: the 19 failures in the CSV/COPY/quarantine/Gate-8 neighbourhood (`test_currency_to_sqlite`, `test_mysql_load_data`, `test_signed_contract_live_sandbox`, `test_sqlite_pg_copy`, `test_stream_append_precount`×3, `test_property7_referential_integrity`×4, `test_property9_row_conservation`×2, `test_property8_decimal_identity`, `test_number_locale_contract`, `test_stream_database_sqlite`×2, `test_validate_judges_transformed_rows`, `test_execute_tracked_csv_to_dynamodb_upsert`) fail identically in a `b68e7c89` worktree (19/19). Largest class: 54× `test_execute_tracked_universal_matrix` — every `database_rest_api_*` / `database_stripe_*` route, `_seed_source` raises `This SaaS connector is currently source-only` (harness seeds through a source-only connector; not a transfer defect). Per-file classes to triage next, all pre-existing on `b68e7c89`: 54×`test_execute_tracked_universal_matrix.py`, 6×`test_sync_mode_matrix.py`, 5×`test_sync_mode_append_vs_overwrite.py`, 4×`test_property7_referential_integrity.py`, 4×`test_execute_tracked_cross_sql_matrix.py`, 3×`test_stream_append_precount.py`, 3×`test_property6_schema_fidelity.py`, 3×`test_oracle_logminer_shared.py`, 3×`test_execute_tracked_file_format_to_warehouse_matrix.py`, 3×`test_catalog_role_readiness.py`, 2×`test_typed_fidelity_transfer_matrix_e2e.py`, 2×`test_stream_database_sqlite.py`, 2×`test_property9_row_conservation.py`, 2×`test_property8_collation_equality.py`, 2×`test_property4_observable_exactly_once.py`, 2×`test_long_token_source_identity.py`, 2×`test_identity_restart_cutover.py`, 2×`test_hana_vertica_wave43.py`, 2×`test_generic_sql_drivername.py`, 2×`test_create_new_temporal_determinism.py`, 2×`test_capability_registry_f7.py`, 1×`test_wave_u_accuracy.py`, 1×`test_wave_aa_accuracy.py`, 1×`test_view_trigger_cutover_matrix.py`, 1×`test_vector_gate8_wave38.py`, 1×`test_validate_judges_transformed_rows.py`, 1×`test_teradata_trino_wave42.py`, 1×`test_sybase_struct_map_wave47.py`, 1×`test_sqlite_pg_copy.py`, 1×`test_source_ha_probe.py`, 1×`test_signed_contract_live_sandbox.py`, 1×`test_shape_unnest_any_source_matrix.py`, 1×`test_qualified_pg_table_read.py`, 1×`test_property8_unicode_form.py`, 1×`test_property8_decimal_identity.py`, 1×`test_property3_source_snapshot_consistent.py`, 1×`test_pilot_llm_wave41.py`, 1×`test_phase_profile.py`, 1×`test_number_locale_contract.py`, 1×`test_netezza_array_wave44.py`, 1×`test_mysql_load_data.py`, 1×`test_mongo_snowflake_validation.py`, 1×`test_module_size_budgets_f8.py`, 1×`test_keyset_pagination_f2.py`, 1×`test_informix_firebird_gate8_wave45.py`, 1×`test_identity_probe_ssot_matrix.py`, 1×`test_file_stream_skip_matrix.py`, 1×`test_file_stream_path.py`, 1×`test_execute_tracked_postgresql_to_postgresql_backfill_widen_fields.py`, 1×`test_execute_tracked_postgresql_to_postgresql_backfill_new_fields.py`, 1×`test_execute_tracked_mysql_to_mysql_backfill_widen_fields.py`, 1×`test_execute_tracked_json_to_postgresql.py`, 1×`test_execute_tracked_csv_to_dynamodb_upsert.py`, 1×`test_enterprise_cdc_vector_create_new.py`, 1×`test_dest_schema_authority_d1.py`, 1×`test_desktop_lab_untested.py`, 1×`test_desktop_lab_dimensions.py`, 1×`test_desktop_lab_cross_matrix.py`, 1×`test_currency_to_sqlite.py`, 1×`test_create_new_snowflake_postgres_types.py`, 1×`test_control_totals_live.py`, 1×`test_code_crosswalk_live.py`, 1×`test_cdc_mysql_to_postgres_dest_owned_delete.py`, 1×`test_cdc_mongodb_change_stream_integration.py`.

Still unmeasured: nothing in §8b. Open: the 152-failure triage above (§3 owners).

## 8c. Append-contract closure across the COPY fleet (2026-08-10, branch `devin/qa-lead-integration`)

Root-cause classes from the 152, fixed at the owner rather than per test.

| # | Root cause (measured) | Owner module | Closure evidence |
|---|-----------------------|--------------|------------------|
| new | **Equal-count "skip complete" on row-addressed destinations.** ~45 COPY fast paths returned success without writing when dest `COUNT(*) == source COUNT(*)` under `full_refresh_append`. A COUNT match is not proof the load happened (source changed under the same cardinality → silent no-op), and Append must accumulate (2 runs of N = 2N — Airbyte/Fivetran contract). PG→PG appended while PG→SQLite/Mongo/S3-dest/Iceberg/DuckDB/Snowflake/BigQuery skipped. | `services/copy_fast_path.skip_complete_identity_copy` is the single owner and documents the contract: only key-addressed destinations (Redis keys, vector ids, object-store objects, compacted Kafka keys) may skip. Every row-addressed route (`copy_*_sqlite`, `copy_*_mongo`/`copy_mongo_sink`, `copy_s3_*`, `copy_*_iceberg`, `copy_duckdb_*`, `copy_snowflake_*`, `copy_bigquery_*`, `copy_sqlite_*`) now declines into an occupied dest with `FastPathUnavailable("…occupied…")` and the row path appends with a measured `target_rows_before`. Per-route `skip_complete_*` helpers deleted (one owner). | 18 route suites re-pointed from `…_skip_when_dest_count_matches` to `…_equal_count_append_declines` (dest read back unchanged at N after the decline). `tests/test_sync_mode_matrix.py`: `full_refresh_append` = 2N on pg/mysql/sqlite/mongodb/redis-as-key-addressed. |
| new | **`target_rows_before` never captured on the stream/file path** (`KeyError: 'target_rows_before'`, 3× `test_stream_append_precount`). | `src/transfer/stream.py` counts the dest before the first write when not resuming (`precount_table`); `src/transfer/file_stream.py` preserves the pre-count through the CSV COPY return. | `tests/test_stream_append_precount.py` 5 passed; Gate-8 append proof closes on `dest_count - target_rows_before`. |
| new | **MongoDB treated as always key-addressed.** Without a mapped `_id` MongoDB assigns ObjectIds, so an append accumulates — yet reconcile used keyed-census (upsert) arithmetic and failed conservation. | `services/primary_key.dest_is_key_addressed(dest_kind, key_columns)` — MongoDB is key-addressed only when a dest key column is `_id`; `stream.py` passes `pk_target_cols`. | Mongo cells of `test_sync_mode_matrix` green (append 2N, upsert N). |
| new | **Redis empty prefix reported the reader envelope (`redis_key/redis_value/redis_type`) as a live schema**, so the writer refused every mapped column as "missing from live schema" on `full_refresh_append`, `incremental_append`, `upsert`. | `src/transfer/endpoint_intelligence.py` + `services/schema_introspect.py`: introspect with `resolve_key_pattern`, an empty prefix has `columns=[]`, `schema={}`; occupied keyspaces keep the fail-closed physical-type check. | `test_sync_mode_matrix.py -k redis`: 5 passed (was 3 failed). |
| new | **SQLite identity COPY moved `'true'/'false'` text verbatim into a BOOLEAN carrier** (D41 class). Tests still expected the fast path on that fixture. | `services/copy_sqlite_common` census declines cells that do not fit the carrier; row path normalizes to 0/1. | `test_stream_database_sqlite.py`: fast path proven on a 0/1 fixture, new `…text_booleans_decline_copy_and_land_as_0_1` reads back `[(0,'integer',125),(1,'integer',125)]`; `test_sqlite_pg_copy` synonym test asserts the carrier-census message. |
| new | `pg_mysql_copy_partitions(10_000_000, 4)` — test expected one partition per worker; owner plans ~1M-row partitions (resume granularity, cap 32). | `services/copy_pg_mysql` unchanged; stale expectation corrected (`10`, and `1_000_000 → 4`). | `test_mysql_load_data.py` green. |

Measured on this head: changed-test + neighbourhood selection (69 files: every re-pointed route suite, sync-mode matrix, precount, redis, introspect, fast path, primary key, row conservation) **580 passed / 0 failed / 8 skipped** with PG/MySQL/Mongo/Redis live. Ruff on the touched files 149 → 139 (all remaining pre-existing BLE001/S110/DTZ001); mypy on the 6 touched modules unchanged at 55 baseline errors.

Open from this wave: untracked `tests/test_cdc_source_population_gate8.py` / `test_cdc_write_pass_digest.py` (recovered from an earlier tree) — `test_cdc_poll_advances_bookmark_and_terminates` fails and the next poll test grows past 5 GB (OOM-killed), i.e. the CDC cursor poll may still re-read the same page when the bookmark cannot advance; not yet fixed, not committed. Remaining 152-classes still open: source-only SaaS seeding (54), MariaDB upsert, RI properties, `_Table.c` stubs, vector Gate-8.

## 9. Closure protocol

For each defect: reproduce on a live engine → fix in the one canonical owner →
re-run the failing cell(s) → read the destination back independently → record
the measured numbers next to the item. Items in §2 are closed by measurement
only; items in §3 and the open half of §5 stay open with their reason and are
never counted as green.
