"""100K-row relational scale matrix (PostgreSQL / MySQL / SQL Server / SQLite / Oracle).

Env-gated: nothing here executes unless ``DATAFLOW_SCALE_MATRIX=1`` and the
engines are actually listening. See ``docs/SCALE_MATRIX_SQL.md``.
"""
