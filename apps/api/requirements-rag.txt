# Optional RAG / embedding extras.
# Install when you need chromadb or sentence-transformers:
#   pip install -r requirements-rag.txt
#
# Defaults: in-memory vector store + TF-IDF embedding fallback
# (DATAFLOW_VECTOR_STORE_BACKEND=memory). Set chromadb via:
#   DATAFLOW_VECTOR_STORE_BACKEND=chromadb

sentence-transformers>=2.2.0
# Pin ChromaDB below 1.0 while the pre-auth code-injection vulnerability
# (CVE-2026-45829 / GHSA-f4j7-r4q5-qw2c) in 1.x server-mode endpoints is
# unpatched. DataFlow only uses the embedded PersistentClient.
chromadb>=0.5.0,<1.0
